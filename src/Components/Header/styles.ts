import styled from 'styled-components'

export const StyledDiv = styled.div`
    position: absolute;
    z-index: 99;
    display: flex;
    justify-content: space-between;
    min-height: 32px;
    width: 100%;
`

